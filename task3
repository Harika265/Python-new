# Task 2

# i) Read inputs
name = input("Enter name: ")
zone = input("Enter zone: ")
empid = input("Enter empid: ")

# Check and convert empid to integer
print("Data type of empid before conversion:", type(empid))
empid = int(empid)
print("Data type of empid after conversion:", type(empid))

# ii) Store in tuple and print
t = (name, zone, empid)
print("Empid from tuple:", t[2])
l = [name, zone, empid]
print("empid from list:",l[2])
print("Empid from tuple:", t[2])
